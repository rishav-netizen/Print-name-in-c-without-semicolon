//? Write your name in c without using a semicolon
#include <stdio.h>

int main()
{
    if (printf("Rishav\n")) // printf returns number of chars in string so its basically if(True)
    {
        return 0;
    }
}